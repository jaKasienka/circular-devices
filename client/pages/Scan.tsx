import PlaceholderPage from "@/components/layout/PlaceholderPage";

export default function Scan() {
  return (
    <PlaceholderPage
      title="New Scan"
      description="Scan a device to get an instant valuation. Keep prompting to build this out."
    />
  );
}
